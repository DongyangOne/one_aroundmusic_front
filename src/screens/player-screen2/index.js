// ** React Imports
import { useEffect, useState } from 'react';

// ** Component Imports
import PlayerScreenView2 from './player-screen2';

const PlayerScreen2 = ({ route, navigation }) => {
  return <PlayerScreenView route={route} navigation={navigation} />;
};

export default PlayerScreen2;
